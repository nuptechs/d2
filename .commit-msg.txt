hardening round 2: health checks, memory caps, WS limits, rate split, tests

- /health and /ready now probe storage, return 503 on failure
- EventCorrelator: cap at 50K events + 5K groups (oldest-evict)
- WebSocket: max 50 connections per IP with cleanup on disconnect
- Rate limiter split: read 200req/s vs write 50req/s
- Export StorageCircuitBreaker + isTransientError for testing
- NEW: circuit-breaker.test.ts (18 tests: CB states, transient errors)
- NEW: security-headers.test.ts (5 tests: CSP, HSTS, X-Frame, etc.)
- NEW: reports.test.ts (6 tests: HTML/JSON/Markdown generation, 404, 400)
- 188/188 tests passing, 10/10 builds
